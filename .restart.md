# Hoglah Restart State

**Date**: 2026-06-12 (initial)

## Current Status
- Requirements specification captured verbatim in `docs/requirements-v1.0.md`
- Project brief, architecture decisions (early ADRs + DQs), and CONTRIBUTING.md written
- Full README.md published with value prop, API sketch, and status
- pyproject.toml + minimal src/hoglah/ (with __init__, py.typed, cli stub)
- Registry update instructions + full desired registry snapshots stored in `docs/`
- GitHub repo exists and is in sync (single initial commit + these metadata additions)

## Next Immediate Work
1. Define core data models (Job, JobResult, JobStatus enum, config).
2. Implement minimal SQLite-backed job store.
3. Implement the Hoglah client (submit, get, list, cancel, wait).
4. Basic Ollama adapter (support prompt + messages).
5. Single-worker execution loop (concurrency=1 default).
6. In-process callback dispatch.
7. CLI commands wired to the library.
8. Tests for persistence + restart behavior (using mocks).

## Open Questions (see docs/architecture-decisions.md for details)
- Exact public client class / factory shape.
- Config mechanism details (Pydantic settings?).
- Retry / interrupted-job recovery policy.
- Context window handling strategy in V1.

## Key Files
- docs/requirements-v1.0.md (source of truth for initial spec — do not overwrite)
- docs/architecture-decisions.md
- README.md (public face)
- pyproject.toml

## How to pick up work
`cd ~/domains/Hoglah && python -m venv .venv && .venv/bin/pip install -e ".[dev,cli]" && .venv/bin/hoglah`

Add decisions to architecture-decisions.md (append-only) and keep .restart.md surgically updated.
